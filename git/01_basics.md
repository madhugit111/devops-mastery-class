Important


GitHub does NOT track empty folders.

If your folders are empty (no files inside), they will NOT appear on GitHub.

That’s probably the real issue.

If you created folders but didn’t create .md files inside them, Git ignores them.